## 1. 服务端文件集能力

- [x] 1.1 新增或暴露读取单个 skill 完整文件集的服务端能力，并返回文件大小、可编辑状态和只读原因
- [x] 1.2 扩展文件草稿校验，支持新增/删除后的完整文件集校验、重复路径检测和 `SKILL.md` 入口文件约束
- [x] 1.3 扩展已有 skill 更新逻辑，使其支持接收完整 `SkillFileDraft[]` 并通过 staging/backup/rollback 原子写回目标目录
- [x] 1.4 更新 `/api/skills/update` 请求校验和响应结构，支持多文件保存并保持现有单文件保存兼容

## 2. AI 多文件能力

- [x] 2.1 扩展 AI 生成和定稿流程，使其可返回包含 `SKILL.md` 与子目录文件的多文件草稿
- [ ] 2.2 扩展基于聊天记录更新已有 skill 的流程，将当前完整文件集提供给 AI 并接收多文件草稿预览
- [ ] 2.3 扩展选区 + 提示词改写流程，支持当前文件局部替换以及按提示返回跨文件草稿预览

## 3. 前端多文件编辑

- [ ] 3.1 在 skill 预览弹窗打开时加载完整文件集，并初始化以 `SKILL.md` 为默认选中文件的编辑状态
- [ ] 3.2 在编辑界面增加文件路径列表，展示大小、可编辑状态和只读原因，支持切换当前文件并保留其他文件草稿修改
- [ ] 3.3 增加新增文件和删除文件交互，保存前仅修改当前草稿并支持重置
- [ ] 3.4 调整保存、重置、未保存状态判断和成功回填逻辑，使其基于完整文件草稿工作
- [ ] 3.5 调整选区 AI 改写逻辑，使其只作用于当前文件，并在切换文件时清理旧选区和预览

## 4. 验证

- [ ] 4.1 验证复杂 skill 子目录文件可查看、新增、删除、编辑、保存，并确认单文件 skill 旧流程仍可用
- [ ] 4.2 验证文件大小和只读原因展示，以及只读文件无法被编辑但不会被静默丢失
- [ ] 4.3 验证 AI 可基于聊天记录和提示词生成/更新多文件 skill，并可基于选区执行当前文件或跨文件改写预览
- [ ] 4.4 运行项目现有检查命令，修复发现的问题
